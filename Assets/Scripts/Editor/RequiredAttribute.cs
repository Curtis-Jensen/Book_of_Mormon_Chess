using UnityEngine;

public class RequiredAttribute : PropertyAttribute 
{
    // This is empty because it's just a marker attribute
    // Unity will use this to identify which fields should be validated
}